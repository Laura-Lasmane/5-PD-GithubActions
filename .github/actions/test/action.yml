inputs:
  environment:
    description: 'Testing environment'
    default: 'dev'
runs:   
  using: "composite"
  steps:
    - name: Print output
      working-directory: course-js-api-framework
      run: |
        echo Testing on ${{ inputs.environment }} has started..
        npm install
        npm run greetings greetings_${{ inputs.environment }}
      shell: cmd